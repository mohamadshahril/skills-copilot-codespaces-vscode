Proposed commit message:

feat(scaffold): add minimal Node.js scaffold, tests, CI and contributor docs

- add `src/` with greet library and CLI
- add `tests/` with Jest tests
- add `package.json`, `.gitignore`, and `README.md`
- add `LICENSE`, `CONTRIBUTING.md`, and PR template
- add GitHub Actions workflow for running tests
